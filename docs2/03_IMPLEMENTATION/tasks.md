# Задачи

**Дата среза:** 2026-09-02. Статусы синхронизированы со [status.md](status.md).

> **Важно.** Часть задач получила статус `PARTIAL`: код для них написан 2026-09-01 (`b53c391`), **но их блокеры не закрыты**, а критерии готовности выполнены не полностью. Наличие файла не означает, что задача сделана. Перечень расхождений и решений, выбранных разработчиком вместо БА, — [status.md](status.md), разделы «Расхождения кода с документацией» и «Решения, принятые разработчиком вместо БА». Критерии готовности в карточках ниже **не смягчались** под фактический код.

Правило выбора задачи: берите задачу со статусом `READY` и без незакрытых блокеров. Задача со статусом `BLOCKED` в работу не берётся — её блокер требует внешнего решения, и обойти его, выбрав значение самостоятельно, запрещено.

## Реестр

| ID | Задача | Требования/GAP | Статус | Зависимости | Результат |
|---|---|---|---|---|---|
| [T-001](#t-001) | Инициализация репозитория и структуры каталогов | GAP-002, GAP-017 (техническая) | **PARTIAL** | — | Git есть (`b53c391`); структура не по ПЗ §16, `requirements.txt` не по D-007 |
| [T-002](#t-002) | Pydantic-схемы доменной модели | REQ-019, GAP-002 | BLOCKED | T-001; OQ-027 (частично), OQ-024, OQ-030, TQ-02 | `app/models/schemas.py` |
| [T-003](#t-003) | Логирование pipeline | REQ-024, GAP-013 (техническая) | READY | T-001 | Модуль логирования, формат ПЗ §19 |
| [T-004](#t-004) | CLI и оркестрация pipeline | REQ-001, REQ-022, REQ-023, GAP-001 | BLOCKED | T-002, T-016; OQ-015 | `app/main.py` существует как прототип с URL-константой; CLI и запись артефактов отсутствуют |
| [T-005](#t-005) | Валидатор и нормализатор URL | REQ-002, REQ-003, REQ-005, REQ-026, GAP-003 | BLOCKED | T-002; OQ-024, OQ-013, OQ-025 | `app/crawler/url_validator.py` |
| [T-006](#t-006) | HTTP-загрузчик | REQ-004, REQ-026, GAP-004 | **PARTIAL**, блокеры не сняты | T-005, T-016, T-017; OQ-007 (частично), OQ-025 | `app/fetcher.py` — есть; robots.txt, rate limiting, retry, лимиты размера отсутствуют |
| [T-007](#t-007) | Парсер HTML | REQ-006, REQ-007, GAP-005 | **PARTIAL**, блокеры не сняты | T-006; OQ-018, OQ-016 | `app/html_parser.py` — есть; критерий успеха и фолбэк не определены |
| [T-008](#t-008) | Извлечение ссылок | REQ-008, GAP-006 | **PARTIAL**, блокер не снят | T-007; OQ-013 | `app/link_extractor.py` — есть; поля не по ПЗ §8, `same_domain` выбран разработчиком |
| [T-009](#t-009) | Классификатор ссылок | REQ-009, GAP-007 | **PARTIAL** | T-008 — PARTIAL; блокирующих OQ нет, но заведены OQ-031…OQ-034 | `config/link_taxonomy.yaml` + `app/taxonomy.py` — есть; крит. 1, 3, 5, 6 не выполнены |
| [T-010](#t-010) | Рекурсивный обход с лимитами | REQ-010, REQ-011, REQ-012, REQ-013, GAP-008 | BLOCKED | T-009, T-017; OQ-025, OQ-029, поведение на лимите | `app/discovery/crawler.py` |
| [T-011](#t-011) | Парсер PDF | REQ-014, REQ-015, GAP-009 | BLOCKED | T-006; OQ-017 | `app/parsers/pdf_parser.py` |
| [T-012](#t-012) | Оценка релевантности документа | REQ-016, REQ-017, REQ-027, REQ-028, GAP-010, GAP-015 | BLOCKED | T-007, T-011, T-016; OQ-006, OQ-028, TQ-01, TQ-03 | `app/research/relevance.py`, LLM-клиент |
| [T-013](#t-013) | Извлечение evidence | REQ-018, REQ-027, REQ-028, GAP-011, GAP-015 | BLOCKED | T-012; OQ-006, OQ-028, OQ-030, TQ-03 | `app/research/evidence_extractor.py` |
| [T-014](#t-014) | Формирование итогового JSON | REQ-019, GAP-012 | BLOCKED | T-013; OQ-027 (частично), OQ-024, OQ-030, TQ-02 | `research_result.json` |
| [T-015](#t-015) | Синтез ответа и Markdown-отчёт | REQ-020, REQ-021, GAP-012 | BLOCKED | T-014; OQ-006, OQ-011, OQ-028, OQ-030 | `app/research/synthesizer.py`, `research_report.md` |
| [T-016](#t-016) | Конфигурация и секреты | REQ-022, REQ-026, REQ-028, GAP-001 | BLOCKED (частично разблокирована) | T-001; OQ-007 (частично), OQ-028 | `config/settings.yaml`, `.env.example`, `config/link_taxonomy.yaml` |
| [T-017](#t-017) | Кэш загрузок | REQ-025, GAP-014 | BLOCKED | T-005; OQ-014, OQ-020, OQ-025 | Модуль кэша |
| [T-018](#t-018) | Тестовый датасет и фикстуры | REQ-029, REQ-030, GAP-016 | BLOCKED | T-001; OQ-010 | `tests/fixtures/`, файл датасета |
| [T-019](#t-019) | Интеграционные и приёмочные тесты | REQ-031, REQ-034, AC-01…AC-13, GAP-016 | BLOCKED | T-015, T-018; OQ-009, OQ-022 | `tests/` — полный набор |

Технические задачи без прямого требования: **T-001** (нет кода — нет ни репозитория, ни структуры; предпосылка любой другой работы, GAP-017) и **T-003** (REQ-024 существует, но задача выделена отдельно, поскольку логирование — сквозной механизм, а не функция pipeline).

---

## T-001

**Инициализация репозитория и структуры каталогов** — статус `PARTIAL`

**Что выполнено (2026-09-02, `b53c391`).** Крит. 1 — `git rev-parse --abbrev-ref HEAD` → `main`. Крит. 3 — `.gitignore` закрывает `.env`, `__pycache__/`, `data/results/*`; `.venv` закрыт собственным `.venv/.gitignore`.

**Что не выполнено.** Крит. 2 — структура плоская (`app/fetcher.py`, `app/html_parser.py`, …) вместо `app/crawler/`, `app/parsers/`, `app/discovery/`, `app/models/`, `app/research/`; нет `data/raw/`. Крит. 4 — в `requirements.txt` нет `PyMuPDF` и `pydantic`, зато добавлены `truststore`, `trafilatura`, `PyYAML`, `lxml_html_clean` без решения. Крит. 5 — `app/**/*.py` содержат логику (задача выполнялась не первой). Версия Python не зафиксирована — [OQ-019](../01_PRODUCT/open-questions.md#oq-019).

**Цель.** Создать пустой, но валидный каркас проекта, чтобы дальнейшая работа велась в системе контроля версий и по утверждённой структуре.

**Границы.** Только каталоги, служебные файлы и репозиторий. Никакой бизнес-логики, никаких модулей pipeline.

**Затрагиваемые компоненты.** Корень проекта; дерево `app/`, `tests/`, `data/raw`, `data/results`, `config/`; `.gitignore`; `requirements.txt` (создание файла, состав — по D-007).

**Зависимости.** Нет.

**Критерии готовности.**
1. `git rev-parse --abbrev-ref HEAD` возвращает имя ветки без ошибки.
2. Дерево каталогов соответствует ПЗ §16 (D-009) — все каталоги существуют.
3. `.gitignore` исключает `data/raw/`, `data/results/`, `.env`, `__pycache__/`, виртуальное окружение.
4. `requirements.txt` содержит зависимости из D-007: `httpx`, `beautifulsoup4`, `lxml`, `PyMuPDF`, `pydantic`.
5. Ни один файл `app/**/*.py` не содержит логики — только пустые модули или отсутствуют.

**Необходимые тесты.** Не требуются: задача не содержит логики.

**Документация к обновлению.** [status.md](status.md) — статус T-001 и GAP-017; [as-is-gap-analysis.md](../02_TECHNICAL/as-is-gap-analysis.md) — колонка «Доказательство» получает первый commit.

**Открытые вопросы и блокеры.** [OQ-019](../01_PRODUCT/open-questions.md#oq-019) блокирует только фиксацию версии Python (`python_requires`, версии пакетов под 3.11+). Выполните эту часть последней; если ответа нет — оставьте версию незафиксированной с пометкой TODO и ссылкой на OQ-019, не выбирая версию самостоятельно.

## T-002

**Pydantic-схемы доменной модели** — статус `BLOCKED`

**Цель.** Описать все структуры данных pipeline как Pydantic-модели, чтобы валидация происходила на границах модулей (принцип A-6).

**Границы.** Только схемы и валидаторы полей. Никакой бизнес-логики обхода, парсинга или оценки.

**Затрагиваемые компоненты.** `app/models/schemas.py`. Сущности перечислены в [domain-model.md](../02_TECHNICAL/domain-model.md): `ResearchRequest`, `UrlCheckResult`, `RawDocument`, `ParsedPage`, `ParsedPdf`, `PdfPage`, `Link`, `ClassifiedLink`, `RelevanceAssessment`, `Evidence`, `ResearchResult`, `Source`, `DiscoveredDocument`, `CacheEntry`.

**Зависимости.** T-001.

**Критерии готовности.**
1. Каждая сущность из `domain-model.md` представлена моделью.
2. Закрытые перечни ПЗ реализованы как enum: класс ссылки (4 значения, ПЗ §9), класс релевантности (3 значения, ПЗ §12).
3. Обязательность каждого поля соответствует ответу на OQ-027 — **не** выбрана разработчиком.
4. Ни одно поле, отсутствующее в ПЗ и не помеченное `PROPOSAL` в `domain-model.md`, не добавлено.
5. Модели импортируются без ошибок.

**Необходимые тесты.** Unit: сериализация и десериализация примеров JSON из ПЗ §6, §7, §8, §11, §12, §13, §14 — каждый пример должен валидироваться соответствующей моделью.

**Документация к обновлению.** [domain-model.md](../02_TECHNICAL/domain-model.md) — снять пометки «не задано»; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-027](../01_PRODUCT/open-questions.md#oq-027) (обязательность полей, конфликт C-007 `source` против `source_url`), [OQ-024](../01_PRODUCT/open-questions.md#oq-024) (перечень `status`), [TQ-02](../02_TECHNICAL/questions-for-techlead.md#tq-02) (объединение моделей документа).

## T-003

**Логирование pipeline** — статус `READY`

**Цель.** Обеспечить сквозное логирование действий crawler и ошибок (REQ-024, BR-015).

**Границы.** Только механизм логирования и его конфигурация. Вызовы логгера добавляются в модулях по мере их реализации.

**Затрагиваемые компоненты.** Модуль логирования; вызов инициализации в `app/main.py` (появится в T-004).

**Зависимости.** T-001.

**Критерии готовности.**
1. Поддержаны типы событий из ПЗ §19: `FETCH`, HTTP-статус, `DISCOVERY`, `FILTER`, `FETCH PDF`, `PARSE`, `RELEVANCE`, `EVIDENCE`.
2. Ошибки записываются с контекстом (URL, стадия).
3. Формат сообщений соответствует примеру ПЗ §19.
4. Логгер не проглатывает исключения — BR-001 реализуется в вызывающем коде, а не в логгере.

**Необходимые тесты.** Unit: проверка формата строки для каждого типа события.

**Документация к обновлению.** [status.md](status.md).

**Открытые вопросы и блокеры.** Блокеров нет. [TQ-05](../02_TECHNICAL/questions-for-techlead.md#tq-05) уточняет транспорт и уровни логов; при отсутствии ответа реализуйте вывод в консоль средствами стандартного `logging` и не считайте это решением — зафиксируйте TODO со ссылкой на TQ-05.

## T-004

**CLI и оркестрация pipeline** — статус `BLOCKED`

**Цель.** Дать точку входа `python -m app.main --question ... --urls urls.json` и последовательный вызов стадий с записью результатов.

**Границы.** Разбор аргументов, чтение входного файла, вызов стадий, запись двух артефактов. Логика стадий — в их собственных задачах.

**Затрагиваемые компоненты.** `app/main.py`, каталог `data/results/`.

**Зависимости.** T-002 (схемы), T-016 (конфигурация); фактически завершается последней, после T-015.

**Критерии готовности.**
1. Команда из ПЗ §27 выполняется и завершается кодом 0 на корректном входе.
2. Создаются `data/results/research_result.json` и `data/results/research_report.md` (REQ-023).
3. Вход соответствует формату из ответа на OQ-015.
4. Отказ отдельного URL не прерывает выполнение (BR-001).
5. Логирование инициализируется до первого сетевого запроса (T-003).

**Необходимые тесты.** Integration: запуск на локальных фикстурах без сети; проверка появления обоих файлов. Acceptance: AC-01, AC-12, AC-13.

**Документация к обновлению.** [api-contracts.md](api-contracts.md) — зафиксировать финальный CLI-контракт; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-015](../01_PRODUCT/open-questions.md#oq-015) — формат `urls.json` и способ передачи вопроса.

## T-005

**Валидатор и нормализатор URL** — статус `BLOCKED`

**Цель.** Проверять допустимость и доступность URL, нормализовать их и формировать `UrlCheckResult`.

**Границы.** Валидация, нормализация, проверка доступности. Загрузка тела ответа — T-006.

**Затрагиваемые компоненты.** `app/crawler/url_validator.py`.

**Зависимости.** T-002.

**Критерии готовности.**
1. Схемы кроме `http` и `https` отклоняются; `file://`, localhost и приватные IP-диапазоны отклоняются (BR-010).
2. Заполняются поля `url`, `status`, `http_status`, `final_url`, `content_type`, `domain` (REQ-002).
3. Обрабатываются все ситуации ПЗ §6: 200, redirect, 404, 403, timeout, SSL error, unsupported content type.
4. Значения `status` соответствуют перечню из ответа на OQ-024.
5. Нормализация реализует алгоритм из ответа на OQ-025.
6. Отказ одного URL не прерывает проверку остальных (BR-001).

**Необходимые тесты.** Unit: по одному кейсу на каждую ситуацию ПЗ §6 (на моках); отклонение `file:///etc/passwd`, `http://localhost`, `http://127.0.0.1`, `http://10.0.0.1`, `http://[::1]`; нормализация дублей вида `https://host/x`, `https://host/x/`, `https://host/x#frag`.

**Документация к обновлению.** [state-machine.md](../02_TECHNICAL/state-machine.md) — снять пометку `PROPOSAL` с перечня состояний URL; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-024](../01_PRODUCT/open-questions.md#oq-024), [OQ-013](../01_PRODUCT/open-questions.md#oq-013), [OQ-025](../01_PRODUCT/open-questions.md#oq-025).

## T-006

**HTTP-загрузчик** — статус `PARTIAL`, блокеры не сняты

**Что есть (`app/fetcher.py`, 26 строк).** httpx + async, `follow_redirects=True`, `final_url` фиксируется (крит. 1, 3). TLS — через `truststore.SSLContext`, проверка сертификата не отключена.

**Чего нет.** Крит. 2 — timeout зашит в код (`timeout=20.0`), лимиты размера отсутствуют. Крит. 4 — robots.txt не запрашивается вовсе. Крит. 5 — User-Agent `regulatory-research-agent/0.1` вместо предписанного BR-016 `RegulatoryResearchBot/0.1`. Крит. 6 — ни задержек, ни `Retry-After`, ни backoff. Крит. 7 — `config/settings.yaml` не существует. Крит. 8, 9 — ни кэша, ни логирования.

**Модуль уже ходит на сайты регуляторов без соблюдения BR-016 и BR-017.** До приведения в соответствие реальные прогоны проводить нельзя.

**Цель.** Загружать HTML и PDF с соблюдением лимитов безопасности (REQ-004, REQ-026).

**Границы.** Сетевой слой и сохранение в `data/raw/`. Разбор содержимого — T-007, T-011.

**Затрагиваемые компоненты.** `app/crawler/fetcher.py`, `data/raw/`.

**Зависимости.** T-005, T-016, T-017 (кэш вызывается из загрузчика).

**Критерии готовности.**
1. Используется `httpx` (D-007).
2. Применяются timeout, max response size, max PDF size из ответа на OQ-007 — значения берутся из конфигурации, а не зашиты в код. **Эти четыре параметра по-прежнему не заданы** (см. блокеры).
3. Редиректы обрабатываются, `final_url` фиксируется (ПЗ §21).
4. **robots.txt соблюдается** (BR-016): проверка перед обходом нового host, кэш 24 ч, 4xx — crawl разрешён, 5xx или сетевая ошибка — host временно блокируется (fail-closed).
5. **User-Agent** — `RegulatoryResearchBot/0.1`, product token `RegulatoryResearchBot`; маскировка под браузер запрещена (BR-016).
6. **Ограничение нагрузки** (BR-017): `per_host_concurrency = 1`, `min_delay_seconds = 2.0`, `global_concurrency = 4`; при 429 соблюдается `Retry-After`; при 429/503/сетевых ошибках — exponential backoff 2→4→8→16 с, `max_attempts = 4`, `max_backoff_seconds = 30`; при продолжающемся ограничении host останавливается, остальные продолжаются (BR-001).
7. Все значения пунктов 4–6 читаются из `config/settings.yaml` (D-047), в код не зашиваются.
8. Повторная загрузка одного URL в рамках исследования не производится (BR-013).
9. Каждая загрузка логируется (BR-015).
10. Обход login, CAPTCHA, paywall и технических access controls не реализуется ни в каком виде (BR-018).

**Необходимые тесты.** Unit на моках: срабатывание timeout; превышение max response size прерывает загрузку; цепочка редиректов даёт корректный `final_url`; повторный запрос обслуживается кэшем; `Disallow` в robots.txt блокирует загрузку; robots.txt с кодом 500 блокирует host, с 404 — не блокирует; два запроса к одному host разделены минимум 2 секундами; ответ 429 с `Retry-After` соблюдается.

**Документация к обновлению.** [integrations.md](../02_TECHNICAL/integrations.md) — таблица технических характеристик INT-01; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-007](../01_PRODUCT/open-questions.md#oq-007) — остались только connect/read timeout, max response size, max PDF size и трактовка «max number of URLs»; [OQ-025](../01_PRODUCT/open-questions.md#oq-025) — канонизация URL. [OQ-008](../01_PRODUCT/open-questions.md#oq-008) закрыт 2026-08-31: вежливость, User-Agent, retry и правовая политика заданы Дополнением к ПЗ §21.

## T-007

**Парсер HTML** — статус `PARTIAL`, блокеры не сняты

**Что есть (`app/html_parser.py`, 33 строки).** title, заголовки, основной текст через `trafilatura` (крит. 5 в части D-008).

**Чего нет.** Крит. 1 — `meta description` не извлекается, полный `ParsedPage` по ПЗ §7 не собран. Крит. 2, 3 — критерий успешного извлечения и фолбэк не определены ([OQ-018](../01_PRODUCT/open-questions.md#oq-018)). Крит. 4 — поведение на не-английской странице не задано ([OQ-016](../01_PRODUCT/open-questions.md#oq-016)); при этом прогон 2026-09-01 выполнялся на венгерском сайте.

**Цель.** Извлекать из HTML title, meta description, заголовки h1–h6 и основной текст без boilerplate (REQ-006, REQ-007).

**Границы.** Только разбор документа. Извлечение ссылок — отдельная задача T-008.

**Затрагиваемые компоненты.** `app/parsers/html_parser.py`.

**Зависимости.** T-006.

**Критерии готовности.**
1. Извлекаются все поля `ParsedPage` (ПЗ §7).
2. Навигация, cookie-баннеры и footer удаляются согласно решению по OQ-018.
3. Реализован фолбэк на случай неудачного извлечения основного текста — по OQ-018.
4. Поведение при не-английской странице соответствует ответу на OQ-016.
5. Библиотеки — `BeautifulSoup4` + `lxml`, при подтверждении D-008 дополнительно `trafilatura`.

**Необходимые тесты.** Unit на сохранённых фикстурах реальных страниц (T-018): извлечение title и заголовков; отсутствие текста меню и footer в поле `text`; корректная работа на странице с пустым `<main>`.

**Документация к обновлению.** [decisions.md](../01_PRODUCT/decisions.md) — перевести D-008 из `PROVISIONAL`; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-018](../01_PRODUCT/open-questions.md#oq-018), [OQ-016](../01_PRODUCT/open-questions.md#oq-016).

## T-008

**Извлечение ссылок** — статус `PARTIAL`, блокер не снят

**Что есть (`app/link_extractor.py`, 65 строк).** Крит. 2 — относительные ссылки абсолютизируются (`urljoin`). Крит. 4 — извлекаются все `<a href>`, кроме не-http(s) схем и дублей пары (url, anchor). Крит. 5 — PDF-ссылки обнаруживаются. Крит. 6 — частично: `dom_container` заполняется только для `nav`/`header`/`footer` (поле `in_nav`), `section_heading` — как ближайший предшествующий заголовок (поле `context`); `surrounding_text` **не заполняется**.

**Чего нет.** Крит. 1 — полей `source_url`, `target_url`, `extension` нет; используются `url`, `anchor_text`, `same_domain`, `in_nav`, `context`. Крит. 3 — `same_domain` вычисляется точным совпадением `netloc`, тогда как [OQ-013](../01_PRODUCT/open-questions.md#oq-013) открыт: правило выбрано разработчиком.

**Цель.** Собрать со страницы все обычные hyperlinks с метаданными (REQ-008, AC-04).

**Границы.** Извлечение и нормализация ссылок. Классификация — T-009.

**Затрагиваемые компоненты.** `app/crawler/link_extractor.py`.

**Зависимости.** T-007.

**Критерии готовности.**
1. Заполняются `source_url`, `target_url`, `anchor_text`, `extension`, `same_domain` (ПЗ §8).
2. Относительные ссылки приводятся к абсолютным.
3. `same_domain` вычисляется по правилу из ответа на OQ-013.
4. Извлекаются **все** обычные hyperlinks страницы (AC-04) — фильтрация на этой стадии не производится.
5. PDF-ссылки на seed-странице обнаруживаются (AC-05).
6. **Заполняется `Link.context`** (добавлено 2026-08-31, [D-039](../01_PRODUCT/decisions.md#d-039)): `surrounding_text` — текст родительского блока; `section_heading` — ближайший предшествующий заголовок h1–h6; `dom_container` — имя ближайшего семантического контейнера (`nav`, `header`, `footer`, `main`, `aside`, `table`, `list`, `body`). Без этих полей классификатор T-009 нереализуем: они входят в обязательные признаки F-06…F-08.

**Необходимые тесты.** Unit на фикстуре: количество извлечённых ссылок совпадает с числом `<a href>` в документе; относительная ссылка разрешена корректно; `extension` для `.pdf` равно `pdf`; `same_domain` для внешней ссылки равно `false`; ссылка в `<nav>` получает `dom_container = nav`, а ссылка под `<h2>` — соответствующий `section_heading`.

**Документация к обновлению.** [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-013](../01_PRODUCT/open-questions.md#oq-013).

## T-009

**Классификатор ссылок** — статус `PARTIAL`

**Что выполнено (2026-09-02, `b53c391`).** `config/link_taxonomy.yaml` создан; классификатор — `app/taxonomy.py` (не `app/discovery/link_classifier.py`); 48 тестов проходят под `py -3.10`. Крит. 2 (`IRRELEVANT` только по положительному признаку), крит. 7 (LLM не вызывается, детерминированность) выполнены.

**Что не выполнено.** Крит. 1 — внутренний класс называется `OTHER`, а не `UNKNOWN` (D-036). Крит. 3 — реализованы не все F-01…F-11. Крит. 4 — зависимость от `research_question` подменена заглушкой из `<title>` ([OQ-034](../01_PRODUCT/open-questions.md#oq-034)). Крит. 5 — `STOPWORDS` и порядок правил живут в Python (`app/taxonomy.py:31`), а не в YAML. Крит. 6 — идентификатор правила не логируется (логирования нет, T-003 не сделана). Крит. 8 — сериализации ещё нет, проверить нельзя. Evaluation dataset из 20 случаев (D-040) отсутствует.

**Заведены постфактум:** [OQ-031](../01_PRODUCT/open-questions.md#oq-031), [OQ-032](../01_PRODUCT/open-questions.md#oq-032), [OQ-033](../01_PRODUCT/open-questions.md#oq-033), [OQ-034](../01_PRODUCT/open-questions.md#oq-034) — четыре правила выбраны разработчиком вместо БА и меняют порядок правил / матрицу, то есть выходят за делегированное полномочие ([link-taxonomy.md §8](../01_PRODUCT/link-taxonomy.md)).

**Цель.** Присвоить каждой ссылке ровно один класс по спецификации [link-taxonomy.md](../01_PRODUCT/link-taxonomy.md) (REQ-009, BR-005).

**Границы.** Только классификация. Решение о переходе по ссылке принимает crawler (T-010) — оно не определено и ждёт [OQ-029](../01_PRODUCT/open-questions.md#oq-029). Изменение структуры таксономии (классы, группы, матрица, порядок правил) в объём задачи **не входит**: это возврат к БА.

**Затрагиваемые компоненты.** `config/link_taxonomy.yaml`, `app/discovery/link_classifier.py`, `tests/fixtures/link_classification_cases.json`.

**Зависимости.** T-008 (источник `Link` с полем `context`) — **не выполнена и заблокирована** по [OQ-013](../01_PRODUCT/open-questions.md#oq-013). См. раздел «Порядок выполнения» ниже.

**Критерии готовности.**
1. Каждая ссылка получает ровно один класс: `HIGH_VALUE`, `POTENTIALLY_RELEVANT`, `NAVIGATION`, `IRRELEVANT` или внутренний `UNKNOWN` (ПЗ §9 «минимум четыре», D-036).
2. `IRRELEVANT` присваивается **только** по положительному признаку (P-1, P-2, P-5 или группа `unrelated_topic`). Ни один путь исполнения не назначает `IRRELEVANT` как остаток — это проверяется тестом.
3. Реализованы все 11 признаков F-01…F-11 и порядок правил P-0…P-6 из [link-taxonomy.md](../01_PRODUCT/link-taxonomy.md). Классификация только по точному совпадению anchor text запрещена.
4. Классификация зависит от `research_question`: парный тест LC-14 / LC-15 на одном и том же входе даёт разные классы.
5. Все строки, шаблоны и списки — из `config/link_taxonomy.yaml` с полем `version`. В Python-коде правил нет; добавление правила не требует изменения кода (D-039).
6. Классификатор возвращает идентификатор сработавшего правила и пишет его в лог (BR-015) — без этого неисполним процесс обновления справочника (D-038).
7. LLM не вызывается (D-037, BR-011). Детерминированность: одинаковый вход — одинаковый выход.
8. `UNKNOWN` не сериализуется в итоговый JSON.

**Необходимые тесты.** Evaluation dataset из 20 случаев ([link-taxonomy.md §12](../01_PRODUCT/link-taxonomy.md), D-040): 20 из 20 дают ожидаемый класс; парный кейс LC-14/LC-15 проходит; для каждого `IRRELEVANT` указан идентификатор правила; доля `UNKNOWN` ≤ 10 %. Дополнительно: пустой anchor text (LC-19); ссылка под несколькими правилами — проверка детерминированного tie-break.

**Порядок выполнения (важно).** Блокирующих открытых вопросов у задачи нет, но T-008 не реализована. Поэтому:

| Часть | Можно делать сейчас | Почему |
|---|---|---|
| `config/link_taxonomy.yaml` v1.0.0 | Да | Зависит только от спецификации |
| `tests/fixtures/link_classification_cases.json` | Да | Синтетические фикстуры, сеть и seed-датасет не нужны |
| Классификатор как чистая функция от признаков | Да | Не требует ни `Link`-модели T-002, ни живого извлечения ссылок |
| Интеграция с реальным потоком ссылок | **Нет** | Ждёт T-008 (и через неё T-007, T-006) |

**Документация к обновлению.** [business-rules.md](../01_PRODUCT/business-rules.md) — BR-005 уже переведён из `BLOCKED`; [status.md](status.md); [link-taxonomy.md](../01_PRODUCT/link-taxonomy.md) — повышение `version` при пополнении справочника.

**Открытые вопросы и блокеры.** Блокирующих нет. Смежные, задачу не останавливающие: [OQ-029](../01_PRODUCT/open-questions.md#oq-029) (что crawler делает с классами — граница T-010), [OQ-013](../01_PRODUCT/open-questions.md#oq-013) (вычисление `same_domain` — граница T-008), [OQ-027](../01_PRODUCT/open-questions.md#oq-027) (регистр значений enum).

## T-010

**Рекурсивный обход с лимитами** — статус `BLOCKED`

**Цель.** Пройти граф ссылок до depth = 2, соблюдая лимиты и дедупликацию (REQ-010…REQ-013).

**Границы.** Управление обходом. Загрузка — T-006, классификация — T-009.

**Затрагиваемые компоненты.** `app/discovery/crawler.py`.

**Зависимости.** T-009, T-017.

**Критерии готовности.**
1. Глубина не превышает 2 с семантикой уровней из BR-002.
2. Соблюдаются `max_documents_per_seed = 20` и `max_total_documents = 50` (BR-003), значения читаются из конфигурации.
3. Один canonical URL не обходится повторно (BR-004) по алгоритму из ответа на OQ-025.
4. Поведение при достижении лимита соответствует принятому продуктовому решению (см. [workflow.md, S-05](../01_PRODUCT/workflow.md#сценарий-s-05-достижение-лимита-обхода)).
5. Какие страницы depth 1 раскрываются на depth 2 и по ссылкам каких классов идёт переход — соответствует ответу на [OQ-029](../01_PRODUCT/open-questions.md#oq-029) (квалификатор «релевантных» в BR-002). Разработчиком не выбирается.
6. Отказ отдельного документа не останавливает обход (BR-001).
7. Обработка `UNKNOWN`-ссылок — по ответу на OQ-029. По умолчанию не реализуется никакой обработки: молчаливое приравнивание `UNKNOWN` к `IRRELEVANT` или к `POTENTIALLY_RELEVANT` запрещено.
8. Соблюдаются ограничения нагрузки BR-017 — обход не обходит их «сверху» собственным параллелизмом.

**Необходимые тесты.** Unit на синтетическом графе: обход останавливается на depth 2; циклическая ссылка не вызывает зацикливания; при 60 доступных документах обработано ровно 50; при 25 документах одного seed обработано ровно 20. Acceptance: AC-06.

**Документация к обновлению.** [business-rules.md](../01_PRODUCT/business-rules.md) — BR-004; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-029](../01_PRODUCT/open-questions.md#oq-029) — политика обхода по классам и судьба `UNKNOWN` (**новый блокер, P1**, заведён при закрытии OQ-012); [OQ-025](../01_PRODUCT/open-questions.md#oq-025); продуктовое решение по поведению на лимите (BR-003).

## T-011

**Парсер PDF** — статус `BLOCKED`

**Цель.** Извлекать постраничный текст из PDF и распознавать сканы (REQ-014, REQ-015).

**Границы.** Только разбор PDF. OCR не реализуется (D-011).

**Затрагиваемые компоненты.** `app/parsers/pdf_parser.py`.

**Зависимости.** T-006.

**Критерии готовности.**
1. Используется `PyMuPDF` (D-007).
2. Заполняются `url`, `title`, `pages[]{page, text}`; определяется количество страниц (ПЗ §11).
3. Нумерация страниц начинается с 1 (как в примере ПЗ §11).
4. PDF без текстового слоя получает `status: ocr_required` по критерию из ответа на OQ-017 и не прерывает обработку (BR-009).
5. Превышение max PDF size обрабатывается на уровне T-006, а не здесь.

**Необходимые тесты.** Unit: text-based PDF даёт непустой текст с корректной привязкой к страницам; PDF-скан из фикстур получает `ocr_required`; повреждённый файл не выбрасывает необработанное исключение. Acceptance: AC-07, AC-11.

**Документация к обновлению.** [decisions.md](../01_PRODUCT/decisions.md) — D-012; [business-rules.md](../01_PRODUCT/business-rules.md) — BR-009; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-017](../01_PRODUCT/open-questions.md#oq-017).

## T-012

**Оценка релевантности документа** — статус `BLOCKED`

**Цель.** Семантически оценить, может ли документ содержать ответ на research question (REQ-016, REQ-017).

**Границы.** Оценка документа. Извлечение цитат — T-013. Включает создание единого LLM-клиента (REQ-028).

**Затрагиваемые компоненты.** `app/research/relevance.py`; модуль LLM-клиента (размещение — [TQ-01](../02_TECHNICAL/questions-for-techlead.md#tq-01)).

**Зависимости.** T-007, T-011, T-016.

**Критерии готовности.**
1. LLM возвращает **только** `relevance_score` (0.0–1.0) и `reason` (Доп. §12.1, D-042). Возвращать класс из LLM запрещено.
2. Класс вычисляет приложение: `>= 0.75 → HIGH`, `>= 0.45 → MEDIUM`, иначе `LOW`. Пороги — из `config/settings.yaml` (`relevance.high_threshold`, `relevance.medium_threshold`), не из кода.
2a. `LOW`-документ не идёт в evidence extraction, но остаётся в research log; `MEDIUM` обрабатывается при наличии остатка лимитов (BR-006).
3. Оценка семантическая: «standard CCyB rate» распознаётся как связанное с «positive-neutral CCyB» (REQ-017, эталонный пример ПЗ §12).
4. Соблюдён BR-011: LLM не применяется к парсингу и HTTP.
5. Обработка отказов LLM соответствует ответу на OQ-006.
6. Стратегия для документов, не помещающихся в контекст, соответствует решению по TQ-03.
7. Доступ к LLM изолирован за единым интерфейсом (REQ-028).
8. Endpoint, имя модели и имя переменной с ключом читаются из конфигурации (D-031): `https://prod-litellm.nationalbank.kz/v1`, ключ — из `.env`. Значения в код не зашиваются. Конкретная модель под эту задачу — по ответу на OQ-028, разработчиком не выбирается.

**Необходимые тесты.** Unit на моках LLM: разбор корректного ответа, разбор невалидного ответа, поведение при таймауте. Integration с реальной моделью на фикстурах — проверка семантического кейса из ПЗ §12. Acceptance: AC-08.

**Документация к обновлению.** [integrations.md](../02_TECHNICAL/integrations.md) — раздел INT-02, контракт LLM; [decisions.md](../01_PRODUCT/decisions.md) — D-017, D-025, D-034; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-006](../01_PRODUCT/open-questions.md#oq-006) (поведение при отказе LLM), [OQ-028](../01_PRODUCT/open-questions.md#oq-028) (модель под задачу), [TQ-01](../02_TECHNICAL/questions-for-techlead.md#tq-01), [TQ-03](../02_TECHNICAL/questions-for-techlead.md#tq-03). Закрыты: [OQ-001](../01_PRODUCT/open-questions.md#oq-001) (D-031), [OQ-003](../01_PRODUCT/open-questions.md#oq-003) (D-042 — пороги 0.75 / 0.45).

## T-013

**Извлечение evidence** — статус `BLOCKED`

**Цель.** Извлечь из релевантных документов фрагменты, отвечающие на вопрос, с обязательной ссылкой на источник (REQ-018).

**Границы.** Извлечение evidence. Итоговый синтез — T-015.

**Затрагиваемые компоненты.** `app/research/evidence_extractor.py`.

**Зависимости.** T-012.

**Критерии готовности.**
1. Обрабатываются только документы с релевантностью `HIGH` и `MEDIUM` (BR-006); `MEDIUM` — при наличии остатка лимитов.
2. Каждый **factual claim** имеет `source_url` (BR-007, D-045). SYSTEM CLAIM (о работе самой системы) освобождён от этого требования, его провенанс — execution log.
3. Для evidence из PDF заполняется `page`, если он доступен (BR-008, AC-11).
4. `quote` — дословная цитата из документа, а не пересказ.
5. **Два шага** (BR-019, D-043): extraction даёт `claim`, `quote`, `source_url`, `page`; отдельный verification-вызов с `temperature = 0` и без доступа к reasoning шага A даёт `support_score`, `verification_reason`, статус.
6. `support_score` **не вычисляется** из `relevance_score` ни равенством, ни умножением (прямой запрет Доп. §13.3).
7. Пороги 0.80 / 0.60 — из конфигурации (`evidence.supported_threshold`, `evidence.weak_support_threshold`). `UNSUPPORTED` не идёт в отчёт как доказательство; `WEAK_SUPPORT` не может быть единственным доказательством factual claim.
8. Схема evidence — единая (D-044): `claim_id`, `claim`, `source_url`, `document_title`, `page`, `quote`, `support_score`, `verification_status`. Поле `confidence` не используется.
9. Обработка отказов LLM — по OQ-006.

**Необходимые тесты.** Unit на моках: документ с LOW не обрабатывается; evidence без `source_url` не проходит валидацию; `page` заполняется для PDF-источника; verification вызывается отдельно от extraction; `support_score = 0.52` при `relevance_score = 0.96` корректно проходит (проверка независимости величин); evidence с `support_score = 0.55` не попадает в отчёт как доказательство. Acceptance: AC-10, AC-11.

**Документация к обновлению.** [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-006](../01_PRODUCT/open-questions.md#oq-006), [OQ-028](../01_PRODUCT/open-questions.md#oq-028), [OQ-030](../01_PRODUCT/open-questions.md#oq-030) (перечень `verification_status`), [TQ-03](../02_TECHNICAL/questions-for-techlead.md#tq-03). Закрыты: [OQ-001](../01_PRODUCT/open-questions.md#oq-001), [OQ-004](../01_PRODUCT/open-questions.md#oq-004) (D-043), [OQ-005](../01_PRODUCT/open-questions.md#oq-005) (D-045).

## T-014

**Формирование итогового JSON** — статус `BLOCKED`

**Цель.** Собрать `research_result.json` по схеме ПЗ §14 (REQ-019).

**Границы.** Сборка и сериализация результата. Markdown-отчёт — T-015.

**Затрагиваемые компоненты.** Сборка `ResearchResult`; запись в `data/results/research_result.json`.

**Зависимости.** T-013.

**Критерии готовности.**
1. Присутствуют все поля ПЗ §14.
2. Счётчики `sources_checked`, `documents_found`, `relevant_documents` согласованы с содержимым массивов.
3. Evidence сериализуется по единой схеме D-044: `claim_id`, `claim`, `source_url`, `document_title`, `page`, `quote`, `support_score`, `verification_status`. Сокращённой проекции §14 больше нет, поле `source` не используется, `confidence` не используется.
4. Значения `status` в `sources[]` соответствуют перечню из OQ-024.
5. Файл валидируется Pydantic-моделью из T-002.
6. Присутствует раздел для claim без достаточного evidence (D-045). Его имя и перечень статусов — по ответу на OQ-030, разработчиком не выбираются.

**Необходимые тесты.** Unit: сгенерированный JSON проходит валидацию схемой; счётчики совпадают с длинами массивов; evidence содержит `support_score`; claim без evidence не попадает в основной ответ. Acceptance: AC-12.

**Документация к обновлению.** [api-contracts.md](api-contracts.md) — зафиксировать финальную схему; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-027](../01_PRODUCT/open-questions.md#oq-027) — остаток (обязательность полей, `page` для HTML, регистр enum), [OQ-024](../01_PRODUCT/open-questions.md#oq-024), [OQ-030](../01_PRODUCT/open-questions.md#oq-030), [TQ-02](../02_TECHNICAL/questions-for-techlead.md#tq-02). Конфликт C-007 разрешён 2026-08-31 (D-044).

## T-015

**Синтез ответа и Markdown-отчёт** — статус `BLOCKED`

**Цель.** Сформировать итоговый краткий ответ и человекочитаемый отчёт (REQ-020, REQ-021).

**Границы.** Синтез и рендеринг отчёта.

**Затрагиваемые компоненты.** `app/research/synthesizer.py`; `data/results/research_report.md`.

**Зависимости.** T-014.

**Критерии готовности.**
1. Структура отчёта соответствует ПЗ §15: секция на каждый seed URL со статусом, список найденных релевантных документов, findings, evidence с цитатой, источником и номером страницы.
2. Поле `answer` итогового JSON заполнено (REQ-021).
3. Язык вывода соответствует ответу на OQ-011.
4. Каждый factual claim в отчёте сопровождается ссылкой на источник (BR-007). SYSTEM CLAIM выводятся без внешней ссылки.
5. **Claim без достаточного evidence не превращается в утвердительное высказывание** (D-045): используется оговорочная формулировка вида «The reviewed source indicates that … ; the exact adoption decision was not identified» со ссылкой на найденный источник. Это проверяемое требование, а не стилистическая рекомендация.
6. Evidence со статусом `UNSUPPORTED` в отчёт как доказательство не попадает; `WEAK_SUPPORT` не подаётся как единственное доказательство.
7. Обработка отказов LLM — по OQ-006.

**Необходимые тесты.** Unit: рендеринг отчёта из фиксированного `ResearchResult` даёт все обязательные секции; отчёт при нулевом количестве evidence не падает; claim из unresolved-раздела рендерится с оговоркой, а не утвердительно. Acceptance: AC-13.

**Документация к обновлению.** [api-contracts.md](api-contracts.md) — раздел Markdown-отчёта; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-011](../01_PRODUCT/open-questions.md#oq-011), [OQ-006](../01_PRODUCT/open-questions.md#oq-006), [OQ-028](../01_PRODUCT/open-questions.md#oq-028), [OQ-030](../01_PRODUCT/open-questions.md#oq-030) (как называется unresolved-раздел в отчёте). [OQ-001](../01_PRODUCT/open-questions.md#oq-001) закрыт 2026-08-31 (D-031).

## T-016

**Конфигурация и секреты** — статус `BLOCKED` (частично разблокирована 2026-08-31)

**Цель.** Вынести лимиты и параметры pipeline в `config/settings.yaml`, ключи — в `.env` (ПЗ §16).

**Границы.** Загрузка конфигурации и её валидация. Значения лимитов задаются ответами на OQ, а не выбираются здесь.

**Затрагиваемые компоненты.** `config/settings.yaml`, `.env.example`, модуль загрузки настроек.

**Зависимости.** T-001.

**Критерии готовности.**
1. `config/settings.yaml` воспроизводит итоговую конфигурацию MVP из Дополнения к ПЗ §21.10 дословно:

```yaml
crawler:
  user_agent: "RegulatoryResearchBot/0.1"
  robots_product_token: "RegulatoryResearchBot"
  respect_robots_txt: true
  robots_cache_ttl_hours: 24
  per_host_concurrency: 1
  global_concurrency: 4
  min_delay_seconds: 2.0
  max_depth: 2
  max_documents_per_seed: 20
  max_total_documents: 50
  retry: {max_attempts: 4, initial_backoff_seconds: 2, max_backoff_seconds: 30}
relevance:
  high_threshold: 0.75
  medium_threshold: 0.45
evidence:
  supported_threshold: 0.80
  weak_support_threshold: 0.60
```

Недостающие и потому **не подставляемые** параметры: connect/read timeout, max response size, max PDF size ([OQ-007](../01_PRODUCT/open-questions.md#oq-007)). Значения по умолчанию для них не выбираются — отсутствие обязательного параметра даёт ошибку старта (пункт 4).
1a. `config/link_taxonomy.yaml` создаётся в рамках T-009; T-016 отвечает только за его загрузку и валидацию поля `version`.
2. `.env.example` содержит имена переменных для доступа к LLM без реальных значений: базовый URL `https://prod-litellm.nationalbank.kz/v1` и переменную с ключом (D-031).
3. `.env` исключён из репозитория (`.gitignore`, T-001).
4. Конфигурация валидируется при старте; отсутствие обязательного параметра даёт понятную ошибку, а не значение по умолчанию.
5. Имя модели — параметр конфигурации; допустимые значения ограничены пулом D-031 (`Qwen/Qwen3.6-27B-FP8`, `Qwen/Qwen3.5-397B-A17B-FP8`, `Qwen/Qwen3.5-35B-A3B-FP8`). Значение по умолчанию не выбирается разработчиком — по OQ-028.

**Необходимые тесты.** Unit: загрузка корректного файла; ошибка при отсутствии обязательного параметра.

**Документация к обновлению.** [migrations-and-deployment.md](migrations-and-deployment.md); [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-007](../01_PRODUCT/open-questions.md#oq-007) — только timeout и предельные размеры; [OQ-028](../01_PRODUCT/open-questions.md#oq-028) — модель по умолчанию. [OQ-008](../01_PRODUCT/open-questions.md#oq-008) закрыт полностью.

**Что можно делать уже сейчас.** После D-031, D-042, D-043, D-046 и D-047 бо́льшая часть конфигурации задана точными значениями: `.env.example` (базовый URL и имя переменной ключа), блоки `crawler`, `relevance`, `evidence` из §21.10 целиком. Заблокированы четыре параметра OQ-007 и имя модели по умолчанию (OQ-028). Задача целиком в `READY` не переводится, но её незаблокированная доля выросла с одного пункта до трёх блоков конфигурации.

## T-017

**Кэш загрузок** — статус `BLOCKED`

**Цель.** Исключить повторную загрузку одного URL в рамках исследования и сохранить метаданные загрузок (REQ-025, BR-013).

**Границы.** Хранение и чтение кэша. Политика жизненного цикла определяется ответом на OQ-020.

**Затрагиваемые компоненты.** Модуль кэша; хранилище по решению OQ-014; `data/raw/`.

**Зависимости.** T-005 (нормализация URL как ключ).

**Критерии готовности.**
1. Повторный запрос того же canonical URL обслуживается из кэша, сетевой вызов не производится (BR-013).
2. Хранятся URL, fetch timestamp, status, content hash, parsed text (ПЗ §20).
3. Носитель соответствует решению по OQ-014.
4. Жизненный цикл (границы «одного исследования», TTL, инвалидация) соответствует ответу на OQ-020.
5. Ключ кэша — canonical URL по алгоритму из OQ-025.

**Необходимые тесты.** Unit: второй запрос не идёт в сеть; URL, различающиеся только trailing slash, дают одну запись (после закрытия OQ-025); повреждённая запись кэша не ломает pipeline.

**Документация к обновлению.** [migrations-and-deployment.md](migrations-and-deployment.md) — схема хранения; [decisions.md](../01_PRODUCT/decisions.md) — D-013; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-014](../01_PRODUCT/open-questions.md#oq-014), [OQ-020](../01_PRODUCT/open-questions.md#oq-020), [OQ-025](../01_PRODUCT/open-questions.md#oq-025).

## T-018

**Тестовый датасет и фикстуры** — статус `BLOCKED`

**Цель.** Подготовить утверждённый набор seed URL и офлайн-фикстуры для воспроизводимых тестов (REQ-029, REQ-030).

**Границы.** Данные для тестов. Сами тесты — T-019.

**Затрагиваемые компоненты.** `tests/fixtures/` (сохранённые HTML и PDF), файл датасета, четыре research question из ПЗ §23.

**Зависимости.** T-001.

**Критерии готовности.**
1. Датасет содержит не менее 10 seed URL по Испании, Чехии и Венгрии (ПЗ §22) — состав по ответу на OQ-010.
2. Зафиксированы четыре тестовых вопроса ПЗ §23 дословно.
3. Сохранены офлайн-копии страниц и PDF, чтобы unit-тесты не зависели от сети и от изменений на сайтах регуляторов.
4. Фикстуры включают: text-based PDF, PDF-скан (для проверки BR-009), страницу со ссылками на PDF, страницу без основного текста.
5. Зафиксирована дата снятия фикстур — сайты регуляторов меняются.

**Необходимые тесты.** Задача сама по себе тестов не требует; её результат потребляют T-005…T-015.

**Документация к обновлению.** [testing-strategy.md](testing-strategy.md) — раздел фикстур; [status.md](status.md).

**Открытые вопросы и блокеры.** [OQ-010](../01_PRODUCT/open-questions.md#oq-010) блокирует состав датасета. Инфраструктура фикстур (каталоги, механизм загрузки офлайн-копий) от ответа не зависит и может быть подготовлена заранее.

## T-019

**Интеграционные и приёмочные тесты** — статус `BLOCKED`

**Цель.** Подтвердить выполнение AC-01…AC-13 и обязательного интеграционного теста ПЗ §25.

**Границы.** Тесты сквозных сценариев и приёмки.

**Затрагиваемые компоненты.** `tests/`.

**Зависимости.** T-015 (весь pipeline), T-018.

**Критерии готовности.**
1. Реализован обязательный тест ПЗ §25 (REQ-031): вход — только страница CNB о SyRB; ожидаемая цепочка `SyRB page → SyRB methodology PDF → finding «standard CCyB rate (or positive neutral rate)»`.
2. Реализована проверка AC-09 (REQ-034) для Испании, Чехии и Венгрии — критерий «релевантного вложенного документа» по ответу на OQ-022.
3. Реализована проверка AC-02 на наборе из ответа на OQ-009.
4. Каждый AC из ПЗ §24 имеет соответствующую проверку — сопоставление в [testing-strategy.md](testing-strategy.md).
5. Результаты прогонов зафиксированы с датой; утверждения о прохождении тестов делаются только по фактическому запуску.

**Необходимые тесты.** Задача целиком состоит из тестов.

**Документация к обновлению.** [testing-strategy.md](testing-strategy.md); [status.md](status.md) — раздел «Проверки: что запускалось, а что нет».

**Открытые вопросы и блокеры.** [OQ-009](../01_PRODUCT/open-questions.md#oq-009), [OQ-022](../01_PRODUCT/open-questions.md#oq-022), плюс все блокеры задач T-004…T-015.
